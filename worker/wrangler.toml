name = "worker-rust"
main = "build/worker/shim.mjs"
compatibility_date = "2023-03-22"

[build]
command = "cd ../client && pwd && wasm-pack build --target=web && cd ../worker && cargo install -q worker-build && worker-build --release"

[site]
bucket = "../client/pkg"
